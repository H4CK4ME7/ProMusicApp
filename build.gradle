buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Android ကို Build လုပ်ပေးမည့် Engine Version
        classpath 'com.android.tools.build:gradle:8.0.0'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
